import styled from '@emotion/styled';
import { Flex } from 'antd';

export const GlobalWrapper = styled(Flex)`
    background-color: var(--ant-color-bg-layout);
    padding: 8px;
    border-radius: var(--ant-border-radius-lg);
`;
