export type MatrixScore = {
    eloDiff: number;
    deltaWin: number;
    scoreDiff: number;
    deltaLose: number;
};

export type MatrixScoreRealResult = {
    eloDelta: number;
    score: number;
};
